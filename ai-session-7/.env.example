# Anthropic — required for prompt caching (Groq does not support it)
ANTHROPIC_API_KEY=

# Groq — model routing / generation (step2+)
GROQ_API_KEY=

# Postgres — same kb_articles DB from Sessions 3–5
DATABASE_URL=postgresql://localhost:5432/rag_kb
API_PORT=3000

# Groq — model routing / generation
GROQ_API_KEY=
